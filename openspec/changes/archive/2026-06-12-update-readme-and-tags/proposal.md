## Why

The README only lists two of the four available skills (`docker-modular-stack` and `project-management`), has a typo in the skill name, and is missing `issue-explore` and `vibe-wiki`. Anyone landing on the repo has an incomplete and slightly inaccurate picture of what's available. Additionally the GitHub repository has no topics set, making it undiscoverable via search.

## What Changes

- **README.md** — rewrite the Skills section to list all four skills with one-line descriptions and exact install commands; fix the `docker-modulat-stack` typo
- **GitHub repository topics** — add tags: `ai`, `agents`, `llm`, `claude-code`, `skills`, `agentskills`, `docker`, `project-management`, `devtools`, `productivity`, `wiki`, `issue-tracking`

## Capabilities

### Modified Capabilities

- `readme-documentation`: README now accurately reflects all available skills with correct names, descriptions, and install commands

### New Capabilities

- `github-topics`: Repository is discoverable via GitHub topic search

## Impact

- `README.md`
- GitHub repository metadata (topics) — set via GitHub API/UI
