services:
  mail:
    image: axllent/mailpit:v1.29.7
    restart: unless-stopped
    networks:
      - main
    environment:
      MP_DATABASE: /data/mailpit.db
    volumes:
      - mail_data:/data

volumes:
  mail_data:
